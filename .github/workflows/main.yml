name: Create YouTube Afterglow app

on:
  workflow_dispatch:
    inputs:
      enable_ytuhd:
        description: "Integrate YTUHD"
        type: boolean
        required: true
        default: false

      enable_ryd:
        description: "Integrate Return YouTube Dislikes"
        type: boolean
        required: true
        default: false

      enable_ytabc:
        description: "Integrate YTABConfig"
        type: boolean
        required: true
        default: false

      enable_demc:
        description: "Integrate DontEatMyContent"
        type: boolean
        required: true
        default: false

      ipa_url:
        description: "URL to the decrypted IPA file"
        default: ""
        required: true
        type: string

      display_name:
        description: "App Name (Optional)"
        default: "YouTube Afterglow"
        required: true
        type: string

      bundle_id:
        description: "BundleID (Optional)"
        default: "i.am.kain.ytafterglow"
        required: true
        type: string

      info_note:
        description: "TIP: Learn more about integrations in the README via the link below"
        default: "https://github.com/xuninc/YTAfterglow#tweak-integration-details"
        type: string
        required: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-tweaks:
    uses: ./.github/workflows/_build_tweaks.yml
    with:
      enable_ytuhd: ${{ inputs.enable_ytuhd }}
      enable_ryd: ${{ inputs.enable_ryd }}
      enable_ytabc: ${{ inputs.enable_ytabc }}
      enable_demc: ${{ inputs.enable_demc }}

  package:
    name: Package IPA
    runs-on: macos-latest
    needs: build-tweaks

    steps:
      - name: Checkout source
        uses: actions/checkout@v6

      - name: Download built debs
        uses: actions/download-artifact@v7
        with:
          name: built-debs
          path: ${{ github.workspace }}

      - name: Hide sensitive inputs
        uses: levibostian/action-hide-sensitive-inputs@v1
        with:
          exclude_inputs: bundle_id,display_name,enable_demc,enable_ryd,enable_ytabc,enable_ytuhd,info_note

      - name: Download and validate IPA
        run: |
          wget "${{ github.event.inputs.ipa_url }}" --no-verbose --no-check-certificate -O youtube.ipa
          file_type=$(file --mime-type -b youtube.ipa)
          if [[ "$file_type" != "application/x-ios-app" && "$file_type" != "application/zip" ]]; then
            echo "::error::Validation failed: The downloaded file is not a valid IPA. Detected type: $file_type."
            exit 1
          fi

      - name: Install cyan
        run: pipx install --force https://github.com/asdfzxcvbn/pyzule-rw/archive/main.zip


      - name: Inject tweaks into IPA
        run: |
          tweaks="ytafterglow.deb OpenYoutubeSafariExtension.appex"
          for f in *.deb; do
            [ -f "$f" ] && tweaks="$tweaks $f"
          done
          cyan -i youtube.ipa -o YTAfterglow_unsigned.ipa -uwef $tweaks -n "${{ github.event.inputs.display_name }}" -b ${{ github.event.inputs.bundle_id }}

      - name: Scrub signing artifacts and fix Substrate load paths
        run: |
          mkdir -p ipa_contents
          unzip -q YTAfterglow_unsigned.ipa -d ipa_contents
          APP_DIR=$(find ipa_contents/Payload -maxdepth 1 -name "*.app" -type d | head -1)
          bash scripts/scrub_signing_artifacts.sh "$APP_DIR"
          bash scripts/fix_appex_bundle_ids.sh "$APP_DIR" "${{ github.event.inputs.bundle_id }}"
          bash scripts/fix_substrate_load_paths.sh "$APP_DIR"
          cd ipa_contents
          zip -qr ../YTAfterglow_unsigned_fixed.ipa Payload
          cd ..
          mv YTAfterglow_unsigned_fixed.ipa YTAfterglow_unsigned.ipa
          rm -rf ipa_contents

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: YTAfterglow-unsigned
          path: YTAfterglow_unsigned.ipa
